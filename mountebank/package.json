{
  "name": "test",
  "description": "test service using mocha and supertest.",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "start": "./node_modules/.bin/mb --configfile imposters.ejs --allowInjection true --loglevel debug"
  },
  "dependencies": {
    "mountebank": "^2.1.0"
  }
}
